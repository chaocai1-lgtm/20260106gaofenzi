# Streamlit Cloud Secrets 配置示例
# 
# 在 Streamlit Cloud 上部署时，需要在以下位置配置这些密钥：
# Settings -> Secrets -> Edit Secrets
#
# 将下面的内容复制粘贴到 Secrets 编辑器中，并替换为实际的值

# Neo4j 数据库配置
NEO4J_URI = "neo4j+ssc://your-database-id.databases.neo4j.io"
NEO4J_USERNAME = "neo4j"
NEO4J_PASSWORD = "your-neo4j-password"

# DeepSeek API 配置
DEEPSEEK_API_KEY = "sk-your-deepseek-api-key"

# 可选：Elasticsearch 配置（如果使用）
# ELASTICSEARCH_CLOUD_ID = "your-elasticsearch-cloud-id"
# ELASTICSEARCH_USERNAME = "elastic"
# ELASTICSEARCH_PASSWORD = "your-elasticsearch-password"
